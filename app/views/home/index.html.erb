<h1>Home</h1>
<div>
  <%# Say hello to used and show first_name if logged session %>
  <p>Welcome, <%= @current_user&.first_name || 'Guest' %>!</p>
  <%# Make a logout %>
  <%# If logged session %>
  <% if @current_user %>
    <%= button_to 'Logout', sign_out_delete_auth_path, method: :delete, class: 'btn btn-link p-0' %>
    <%# If not logged session %>
  <% else %>
    <%= link_to 'Login', sign_in_auth_path %>
  <% end %>
</div>
